
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  creationTimestamp: null
  name: helmreleases.application.kubesphere.io
spec:
  group: application.kubesphere.io
  names:
    kind: HelmRelease
    listKind: HelmReleaseList
    plural: helmreleases
    singular: helmrelease
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Release Name
      type: string
    - jsonPath: .spec.workspace
      name: Workspace
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HelmRelease is the Schema for the helmreleases API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: HelmReleaseSpec defines the desired state of HelmRelease
            properties:
              appId:
                description: use chart from helmapplication
                type: string
              appVerId:
                type: string
              chartAppVer:
                description: appVersion from Chart.yaml
                type: string
              chartData:
                description: chart data
                type: Any
              chartName:
                description: chart name
                type: string
              chartVersion:
                description: specify the exact chart version to install. If this is not specified, the latest version is installed
                type: string
              description:
                description: helm release description msg
                type: string
              name:
                description: release name
                type: string
              repoId:
                type: string
              values:
                description: release values.yaml, base64 encoded
                type: Any
              version:
                description: release version
                type: integer
              workspace:
                description: release workspace
                type: string
            required:
            - chartName
            - chartVersion
            - name
            - version
            type: object
          status:
            description: HelmReleaseStatus defines the observed state of HelmRelease
            properties:
              deployStatus:
                items:
                  properties:
                    deployTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    state:
                      description: last deploy state
                      type: string
                  required:
                  - deployTime
                  - state
                  type: object
                type: array
              hash:
                description: md5(Values)
                type: string
              lastDeployed:
                description: last successful deploy time
                format: date-time
                type: string
              lastUpdate:
                description: last update time
                format: date-time
                type: string
              message:
                type: string
              state:
                description: current state
                type: string
              version:
                description: current release version
                type: integer
            required:
            - state
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
