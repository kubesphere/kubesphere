apiVersion: network.kubesphere.io/v1alpha1
kind: WorkspaceNetworkPolicy
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
  name: workspacenetworkpolicy-sample
spec:
  # Add fields here
  foo: bar
