apiVersion: v1
kind: Service
metadata:
  name: default-http-backend
  labels:
    app: kubesphere
    component: kubesphere-router
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: kubespshere
    component: kubesphere-router
